{
  "name": "charging-stations",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@9.0.0",
  "workspaces": [
    "server",
    "webApp"
  ],
  "scripts": {
    "build": "cd webApp && npm run build",
    "dev": "concurrently \"cd server && npm run dev\" \"cd webApp && npm run dev\"",
    "install:all": "npm install && cd server && npm install && cd ../webApp && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
